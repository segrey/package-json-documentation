doctype xml
// Generated by https://github.com/segrey/package-json-documentation
entries
  each entry in entries
    entry(name='#{entry.name}')
      if entry.aliases.length > 0
        aliases
          each alias in entry.aliases
            alias.
              !{alias}
      short-description.
        !{entry.shortDescription}
      full-description.
        <![CDATA[!{entry.fullDescription}]]>
